<!DOCTYPE html>
<html>
  <%= link_to 'New Post', new_post_path %>

	<h1>Posts:</h1>
	 
		<% @posts.each do |post| %>
			<p>
				<strong><%= post.title %></strong><br>
				<%= post.text %><br>
				<%= link_to 'Show', post_path(post) %>
			</p>
		<% end %>
	</table>
	</html>
